const {
  create,
  select,
  selectAll,
  getProperty,
  setProperty,
  insertTemplate,
  addStyle,
  toggleClass,
  addClass,
  removeClass,
} = require('./dom.mjs');

const { defineElement } = require('./element.mjs');

module.exports = {
  create,
  select,
  selectAll,
  getProperty,
  setProperty,
  insertTemplate,
  addStyle,
  toggleClass,
  addClass,
  removeClass,
  defineElement,
};
